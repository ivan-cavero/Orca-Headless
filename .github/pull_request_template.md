<!--
Thanks for the pull request. One rule matters more than any other here: changes
ship with evidence. Most of what is in this repository was written after a
container failed in a specific, reproducible way, and the value is in that
specificity. See CONTRIBUTING.md.
-->

## What changed, and why

<!-- The behaviour that was wrong, or the gap that was missing. If this fixes a
bug, describe the failure you observed before the fix. -->

## What it affects

- [ ] `Dockerfile`
- [ ] `entrypoint.sh`
- [ ] `scripts/`
- [ ] Compose files
- [ ] CI
- [ ] Documentation only

## Evidence

<!--
Paste the observed output. Not "it works" — the command and what it printed.

For anything touching how Orca's files are stored or mounted, prove that a
worktree survives a container recreate:

  docker compose exec orca sh -c '
    mkdir -p "$HOME/projects/demo" && cd "$HOME/projects/demo"
    git init -q -b main
    git remote add origin https://github.com/example/demo.git
    git -c user.email=t@t.t -c user.name=t commit -q --allow-empty -m init
    orca-ide repo add --path "$HOME/projects/demo" --json >/dev/null
    orca-ide worktree create --repo "path:$HOME/projects/demo" --name ci-wt --json >/dev/null
    echo MARKER > "$HOME/orca/workspaces/demo/ci-wt/UNCOMMITTED.txt"'
  docker compose down && docker compose up -d
  docker compose exec orca cat /home/orca/orca/workspaces/demo/ci-wt/UNCOMMITTED.txt
-->

```
paste the commands and their output here
```

## Checklist

- [ ] I read [CONTRIBUTING.md](../CONTRIBUTING.md) and ran the checks it lists.
- [ ] The container still reaches `healthy` with the default configuration.
- [ ] If I changed a variable, a path, a default or a command, I updated the
      relevant file under `docs/`.
- [ ] No secrets, pairing URLs, private hostnames or private paths are included.
- [ ] No `Co-Authored-By` or "generated by" attribution was added.
- [ ] If I touched how Orca's files are stored, I verified a worktree survives a
      recreate.
